--- git.org/connect.c.orig	2009-07-02 16:11:27.000000000 +0930
+++ git/connect.c	2009-07-02 16:11:39.000000000 +0930
@@ -605,7 +605,7 @@
 	conn->argv = arg = xcalloc(6, sizeof(*arg));
 	if (protocol == PROTO_SSH) {
 		const char *ssh = getenv("GIT_SSH");
-		if (!ssh) ssh = "ssh";
+		if (!ssh) ssh = "/opt/bin/ssh";
 
 		*arg++ = ssh;
 		if (port) {
