#!/bin/sh

/bin/killall denyhosts 2>/dev/null

umask 077

mkdir -p /opt/var/run
rm -f /opt/var/run/denyhosts.pid

touch /opt/etc/hosts.allow
touch /opt/etc/hosts.deny

/opt/share/denyhosts/denyhosts.py --daemon  -c /opt/etc/denyhosts.cfg
