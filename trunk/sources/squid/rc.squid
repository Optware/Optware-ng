#!/bin/sh

if [ -n "`pidof squid`" ]; then 
    echo "Stopping squid-cache server: "
    /bin/killall squid 
else
    echo "Starting squid-cache server in 60s: "
    /opt/etc/squid/squid.delay-start.sh&
fi

