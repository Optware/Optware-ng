--- a/Makefile.am
+++ b/Makefile.am
@@ -1,8 +1,8 @@
 # Dependencies
 
 JDK_UPDATE_VERSION = 85
-BUILD_VERSION = b02
-COMBINED_VERSION = $(JDK_UPDATE_VERSION)-$(BUILD_VERSION)
+JDK_BUILD_VERSION = b02
+COMBINED_VERSION = $(JDK_UPDATE_VERSION)-$(JDK_BUILD_VERSION)
 
 CORBA_CHANGESET = cbe0edb3d345
 JAXP_CHANGESET = 4b0a1c213416
@@ -579,7 +579,7 @@
 ICEDTEA_ENV = \
 	ALT_JDK_IMPORT_PATH="$(BOOT_DIR)" \
 	ANT="$(ANT)" \
-        BUILD_NUMBER="$(BUILD_VERSION)" \
+        BUILD_NUMBER="$(JDK_BUILD_VERSION)" \
 	JDK_UPDATE_VERSION="$(JDK_UPDATE_VERSION)" \
         JRE_RELEASE_VERSION="@JAVA_VER@_$(COMBINED_VERSION)" \
 	MILESTONE="Optware-ng" \
