--- m4-1.4.16/lib/stdio.in.h.orig	2011-03-01 18:39:29.000000000 +0200
+++ m4-1.4.16/lib/stdio.in.h	2015-02-23 23:10:36.250534531 +0200
@@ -162,7 +162,7 @@ _GL_WARN_ON_USE (fflush, "fflush is not
    so any use of gets warrants an unconditional warning.  Assume it is
    always declared, since it is required by C89.  */
 #undef gets
-_GL_WARN_ON_USE (gets, "gets is a security hole - use fgets instead");
+/*_GL_WARN_ON_USE (gets, "gets is a security hole - use fgets instead");*/
 
 #if @GNULIB_FOPEN@
 # if @REPLACE_FOPEN@
