--- Transmission-0.6/libtransmission/peer.c	2006-12-04 13:15:03.000000000 +0100
+++ transmission/libtransmission/peer.c	2006-12-05 12:46:44.000000000 +0100
@@ -24,8 +24,8 @@
 
 #include "transmission.h"
 
-#define MAX_REQUEST_COUNT 32
-#define OUR_REQUEST_COUNT 8  /* TODO: we should detect if we are on a
+#define MAX_REQUEST_COUNT 64
+#define OUR_REQUEST_COUNT 16  /* TODO: we should detect if we are on a
                                 high-speed network and adapt */
 
 typedef struct tr_request_s
