echo "Note that this is a disk only version of syslog-ng. If found to be stable,"
echo "there will be a flash version started from inittab and with logs moved to /var"
echo "Until then, the config file is kept in /opt/etc/syslog-ng/syslog-ng.conf"
echo
echo "To install this version of syslog-ng, comment out both lines in /etc/inittab and copy"
echo "/opt/doc/syslog-ng/rc.rstimezone to /share/hdd/conf/unslung/rc.rstimezone if the"
echo " destination file does not exist. Otherwise merge the two."
echo "Log files will after this be located under /opt/var/log"
return 0