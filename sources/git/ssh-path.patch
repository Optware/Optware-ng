--- git-1.6.4/connect.c.orig	2009-07-29 01:18:18.000000000 -0700
+++ git-1.6.4/connect.c	2009-07-29 13:04:51.000000000 -0700
@@ -609,7 +609,7 @@
 	if (protocol == PROTO_SSH) {
 		const char *ssh = getenv("GIT_SSH");
 		int putty = ssh && strcasestr(ssh, "plink");
-		if (!ssh) ssh = "ssh";
+		if (!ssh) ssh = "/opt/bin/ssh";
 
 		*arg++ = ssh;
 		if (putty && !strcasestr(ssh, "tortoiseplink"))
