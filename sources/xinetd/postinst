echo "To activate xinetd, copy /opt/doc/xinetd/rc.xinetd to /share/hdd/conf/unslung"
echo "(or /share/{hdd,flash}/{conf,data}/unslung as appropriate for your situation)"
echo "To start xinetd, either restart your system or kill inetd before running"
echo "/opt/etc/init.d/S10xinetd"
echo
echo "Note that telnetd is enabled by default. Edit /opt/etc/xinetd.d/telnetd"
echo "and set 'disable=yes' to disable telnet AFTER making sure that Dropbear"
echo "or other means of logging onto your system are enabled and working."
return 0