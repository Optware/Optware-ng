service swat
{
        disable         = no
        port            = 901
        socket_type     = stream
        wait            = no
        user            = root
        server          = %OPTWARE_TARGET_PREFIX%/sbin/swat
        log_on_failure  += USERID
}
